/*
 * mcdutil.h - Utility functions for music CD database program,
 */
#ifndef MCDUTIL_H_ /* Guard against multiple inclusion */
#define MCDUTIL_H_

/*
 * Get a string to plug into a `key' or a `value'
 */
int mcd_getstr(char buf[], int len);

#endif /* MCDUTIL_H_ */
